export const metadata = {
  title: "Discover Ideas",
  description: "Browse, filter, and find inspiration from a collective vault of forward-thinking concepts.",
};

export default function IdeasLayout({ children }) {
  return children;
}
