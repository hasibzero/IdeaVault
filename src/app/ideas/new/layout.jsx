export const metadata = {
  title: "Add New Idea",
  description: "Share your next-gen concept with the IdeaVault community.",
};

export default function NewIdeaLayout({ children }) {
  return children;
}
