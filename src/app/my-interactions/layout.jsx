export const metadata = {
  title: "My Interactions",
  description: "Track your engagements, discussions, and saved concepts within IdeaVault.",
};

export default function MyInteractionsLayout({ children }) {
  return children;
}
