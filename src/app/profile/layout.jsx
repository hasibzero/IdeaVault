export const metadata = {
  title: "Profile",
  description: "Manage your account, review your saved ideas, and jump back into the vault.",
};

export default function ProfileLayout({ children }) {
  return children;
}
